<%= form_for(@kit_schedule) do |f| %>
  <% if @kit_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kit_schedule.errors.count, "error") %> prohibited this kit_schedule from being saved:</h2>

      <ul>
      <% @kit_schedule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
