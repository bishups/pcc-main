<%= form_for(@change_suggestion) do |f| %>
  <% if @change_suggestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@change_suggestion.errors.count, "error") %> prohibited this change_suggestion from being saved:</h2>

      <ul>
      <% @change_suggestion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description , :class=>'form-control'%>
  </div>
  <div class="field">
    <%= f.label :priority %><br />
    <%= f.select :priority,options_for_select(['Low','Moderate','Critical']), {}, :class => 'form-control' %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
